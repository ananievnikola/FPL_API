- Add [JsonProperty(PropertyName = "xxx")] to all input models

- Review all object type props in the models 

- Add some kind of auth for updating the data

- Add loger

- DI mechanism for the controllers

- Rewrite requesters so the singleton is actually "singleton per user"

--- parallelism in updater

- Paging of the results

- AutoMapper to replace manual mapping
